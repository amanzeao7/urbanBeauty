import { service } from './service'
import { pricing, teamMember, testimonial, siteSettings } from './content'

export const schemaTypes = [
  service,
  pricing,
  teamMember,
  testimonial,
  siteSettings,
]
